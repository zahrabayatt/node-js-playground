// some node package in our application are used only during development for example packages for running unit tests, analysis code, bundling js and these dependencies are development dependencies and they not should go in production development where we deploy our application.

// To Install a package as dev dependency, run this command:
// npm i [package name] --save-dev
// Or
// npm i [package name] -D

// All of the dependencies Whether is a application's dependency or a development dependency are installed and located under the node_module folder.
